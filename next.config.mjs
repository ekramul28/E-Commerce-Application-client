/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.ibb.co",
      "res.cloudinary.com",
      "www.gravatar.com",
      "via.placeholder.com",
    ],
  },
};

export default nextConfig;
